version: '3.8'
services:
  spoolman:
    image: ${GHCR_IMAGE}:test
    environment:
      - SPOOLMAN_LOGGING_LEVEL=INFO
      - TZ=Europe/Stockholm
  tester:
    image: ${GHCR_IMAGE}-tester:latest
    volumes:
      - ./tests:/tester/tests
    environment:
      - DB_TYPE=sqlite
    depends_on:
      - spoolman
